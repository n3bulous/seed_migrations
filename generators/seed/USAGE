Description:
    Creates a new seed data file. Pass a name for the seed data file, 
    either CamelCased or under_scored, as an argument.

    This creates a seed data file in db/seeds.

Example:
    `./script/generate seed administrator_and_countries`

    creates the seed file and directory:
        db/seeds
        db/seeds/20091209014329_administrator_and_countries.rb

